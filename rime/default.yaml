# Rime deployment configuration for CircleOne (isiBheqe soHlamvu)

schema_list:
  - schema: one

menu:
  page_size: 5

ascii_composer:
  switch_key:
    Shift_L: inline_ascii
    Shift_R: commit_text
    Control_L: noop
    Control_R: noop
    Caps_Lock: clear
    Eisu_toggle: clear
